#!/usr/bin/env bash

echo "COMMIT: $CI_COMMIT_ID"
TARGET_PR=`github-get-pull-request $CI_COMMIT_ID 2> \&1`
if [ "$?" -ne 0 ] ; then
  echo "Failed to create comment. $TARGET_PR"
  exit 1
fi
echo "Commenting $TARGET_PR"
github-comment-pull-request $TARGET_PR "$1"

